name: Automated Dependency Updates

on:
  # schedule:
    # Mon-Fri 2:00 AM ET (07:00 UTC)
    # - cron: "0 7 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  dependency-updates:
    runs-on: ubuntu-latest
    steps:
      - name: To do
        run: echo "TODO this"
